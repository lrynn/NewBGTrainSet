/*------------------------------------------------------------------------------
    8500 Switches
------------------------------------------------------------------------------*/

// Switch
// None

switch(FEAT_TRAINS, SELF, sw_8500_loco, cargo_subtype) {
    1:    set_8500;
    set_8500;
}

// Sounds
switch(FEAT_TRAINS, SELF, sw_8500_sound, extra_callback_info1) {
    SOUND_EVENT_START: sound("sounds/FEL8500Start.wav");
    return CB_RESULT_NO_SOUND;
}